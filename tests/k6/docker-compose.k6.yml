version: '3.8'

services:
  k6:
    image: grafana/k6:latest
    container_name: k6-stress-test
    networks:
      - healing-network
    volumes:
      - ./:/scripts

networks:
  healing-network:
    external: true
