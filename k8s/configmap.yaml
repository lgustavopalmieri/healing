apiVersion: v1
kind: ConfigMap
metadata:
  name: healing-specialist
  namespace: healing
  labels:
    app.kubernetes.io/name: healing-specialist
    app.kubernetes.io/component: backend
data:
  APP_ENV: "production"

  SERVER_GRPC_PORT: "50051"
  SERVER_HTTP_PORT: "8080"
  SERVER_METRICS_PORT: "9090"
  SERVER_SHUTDOWN_TIMEOUT: "30s"
  SERVER_MAX_CONNECTIONS: "1000"
  SERVER_CONNECTION_TIMEOUT: "10s"

  POSTGRES_HOST: "postgres.healing.svc.cluster.local"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "healing_specialist_db"
  POSTGRES_SSLMODE: "require"
  POSTGRES_MAX_OPEN_CONNS: "25"
  POSTGRES_MAX_IDLE_CONNS: "5"
  POSTGRES_CONN_MAX_LIFETIME: "5m"
  POSTGRES_CONN_MAX_IDLE_TIME: "10m"

  KAFKA_BOOTSTRAP_SERVERS: "kafka.healing.svc.cluster.local:9092"
  KAFKA_AUTO_OFFSET_RESET: "earliest"

  OTEL_SERVICE_NAME: "healing-specialist"
  OTEL_SERVICE_VERSION: "1.0.0"
  OTEL_ENVIRONMENT: "production"
  OTEL_EXPORTER_OTLP_GRPC_ENDPOINT: "otel-collector.observability.svc.cluster.local:4317"
  OTEL_EXPORTER_OTLP_PROTOCOL: "grpc"

  ELASTICSEARCH_ADDRESSES: "http://elasticsearch.healing.svc.cluster.local:9200"
  ELASTICSEARCH_INDEX_SPECIALISTS: "specialists"
  ELASTICSEARCH_MAX_RETRIES: "3"
  ELASTICSEARCH_RETRY_BACKOFF: "1s"

  LICENSE_VALIDATION_BASE_URL: "http://license-service.healing.svc.cluster.local:8080"
